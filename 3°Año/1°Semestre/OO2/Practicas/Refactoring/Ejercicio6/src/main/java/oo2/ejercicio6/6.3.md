# Bad Smells identificados:

1. ultimosPosts
    * Long Method -> ultimos posts tiene demasiadas responsabilidades
    * Temporary Field -> declaracion de variables temporales no ncesarias

Todo el metodo se puede solucionar un Strem

this.posts.stream().filtrer(post->!post.getUsuario().equals(user)).sorted((p1,p2) -> p2.getFecha().compareTo(p1.getFecha())).limit(cantidad).collect(Collectors.toList());

