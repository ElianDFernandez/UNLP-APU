# Parcial 2017

## Ejercicio 1

```pascal 
    program Ejercicio1;

    type 
        vector = array [1..100] of integer;

    procedure imprimirOcurrencias(v:vector);
    var 
        i:integer;
    begin
        for i:=1 to 100 do begin 
            if (v[i] > 0) then 
                writeln('Valor ', i, ' cantidad de ocurrencias ', v[i]);
        end;
    end;

    procedure inicializarVector(var v:vector);
    var 
        i:integer;
    begin 
        for i:=1 to 100 do 
            v[i]:=0;
    end;

    procedure cargarVector(var v:vector);
    var     
        num:integer;
    begin 
        inicializarVector(v);
        writeln('Ingrese un valor: ');
        readln(num);
        while (num <> 0) do begin 
            v[num]:= v[num] + 1;
            writeln('Ingrese un valor: ');
            readln(num);
        end;
    end;

    var 
        v:vector;
    begin 
        cargarVector(v);
        imprimirOcurrencias(v);
    end.
```
## Ejercicio 2

**Modularizacion**
* A. Defina el concepto de moedularizacion y sus principales ventajes.

    La modularización es una técnica que consiste en descomponer un problema complejo en varias partes más pequeñas y manejables, llamadas módulos. Cada módulo aborda un aspecto específico del problema y opera de manera independiente, pero en conjunto contribuyen a resolver el problema mayor.
    Nos trae ventajas como poder individualizar el trabajo, reutilizar el codigo, mayor escalabilidad, y una mejor legibilidad.

* B. Explique las diferencias entre variable globl, variable local, parametro por valor y por referencia.

    Una variable global, es una varible a la cual podemos acceder desde cualquier punto del programa. Una variable local, tiene un alcance limitado al lugar donde se esta declarando. 
    En la comunicacion de los modulos y funciones aparecen los paremtros, que son variables que se le envian al procedimiento o funcion. Los parametros por valor, son copias de estas variables, esto implica que si se modifican dentro no se modifica la variable.
    Los parametros por referencia, son la direccion de la variable, es decir si se modifica se estaria modificando directamente la varible misma. 

* C. Indique que imprime cada sentencia.

    program uno;
    var
        a,b,c:integer;
    procedure prueba(var a:integer; var b:integer; c:integer);
        var b:integer;
    begin 
        b:=a - 11;
        c:=b + 10;
        a:=a + c+ 5;
        write(a,b,c);
    end;

    begin 
        a:=3;
        b:=10;
        write(a,b,c);
        prueba(b,c,a);
        write(a,b,c);
    end.

    Orden de prioridad: si programa 1° variable local
                                      2° variable global
                          si procedimiento 1° variable local
                                           2° parametro
                                           3° variable global

    1°write(a,b,c) (Pograma principal)
        a -> vl (a:= 3); A IMPRIME 3;
        b -> vl (b:= 10); B IMPRIME 10;
        c -> no declarada
    
    2°prueba -> write(a,b,c) (prodecimiento)
        declaracion prueba (var a, var b, c)
        llamado prueba(->b, ->c, a); prueba(->10, -> ,3);
        variable local b:integer;

        b:= 10 - 11 -> b:=-1;
        c:= -1 + 10 -> c:= 9;
        a:= a + c + 5 -> a:= 24;

        write(a,b,c) -> 24, -1 , 9;

    3° write(a,b,c) programa principal
        b:= 24
        c:= 9
        a:= 3

## Eficiencia 
* A. Define el concepto de eficiencia
    El analisis de eficiencia de un programa estudia el timepo que tarda y la cantiad de memoria que ocupa un programa en ejecutarse.    

* B. Caclule la ocupacion de memoria y el timepo de ejecuccion para el siguiente codigo:

    program calculo;
    type 
        cadenda50 = string[50];
        persona = record 
            nom:cadena50;
            edad:integer;
        end;
    lista = ^reg;
    reg = record 
        datos:persona;
        sig:lista;
    end;
var 
    pri,aux:lista;
    p:persona;
    cant:integer;
begin 
    pri:=nil;
    read(p.nom, p.edad);
    while (p.nom <> 'ZZZ') do begin     
        new(aux);
        aux^.dato:=p;
        aux^.sig:=pri;
        pri:=aux;
        read(p.nom, p.edad);
    end;s
    aux:=pri;
    cant:=0;
    while (aux <> nil ) do begin 
        if (aux^.datos.edad = 18) then cant:= cant +1;
        aux:= aux^.sig;
    end;
end.

**Memoria estatica**:
    Variables globales y locales al programa:
    globales: 
        pri = puntero(4 bytes)
        aux = puntero(4 bytes)
        p = peronsa (51 + 4 bytes = 55)
        cant = integer (4 bytes)

    locales: no hay 
    
    Total = 8 + 55 + 4 = 57 sbytes

**Memoria dinamica**
    reserva y libera memoria
    dentro de while = 
        new(aux) = asigno (persona + sig = 59 bytes)

    dispose no hay no se libera memoria.

    total = x(cant. veces que se repite el while) * 59 bytes

**Tiempo de ejecucion** 
    pri:=nil 1UT
    while Condicion 1UT cuerpo = 1UT + 1UT + 1UT => 1*(N+1)+3UT*N
    aux:=pri 1UT
    cant:=0 1UT
    while condicion 1UT cuerpo = 1UT+2UT + 1UT => 1*(N+1)+4UT*N

    Total = N + 1UT + 3N + 2UT + N + 1UT + 4N


    1 
    n+1+3n -> 4n +1
    2
    n+1+4n -> 5n +1

    9n + 5
    




    


